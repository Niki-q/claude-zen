# Captured browser session — contains claude.ai cookies/tokens, never commit
*.har

# OS / editor junk
.DS_Store
Thumbs.db

# Transient debug captures (console exports, tab error dumps)
console-export-*.log
tab-error.log
